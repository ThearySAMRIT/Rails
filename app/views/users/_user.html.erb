<li>
  <%= gravatar_for user, size: Settings.gravatar_option.size1 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    <%= t ".t" %><%= link_to t(".delete"), user,
     method: :delete, data: {confirm: t(".sure")} %>
  <% end %>
</li>
